{
  "name": "snake-game",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/snake.ts --bundle --outfile=src/snake.js",
    "dev": "esbuild src/snake.ts --bundle --outfile=src/snake.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
